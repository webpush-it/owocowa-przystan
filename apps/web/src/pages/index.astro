---
import { Image } from 'astro:assets'
import heroImg from '../assets/sad_glowne.jpg'
import workshopImg from '../assets/sad_zajecia.jpg'
import workshop2Img from '../assets/sad_zajecia2.jpg'
import { sanityClient, urlFor } from '../lib/sanity';

import Layout from '../layouts/Layout.astro'

// Przykładowe dane "statyczne" do szybkiego startu, zanim backend zostanie uzupełniony
const features = [
    { title: "Edukacja", desc: "Warsztaty przyrodnicze dla dzieci i dorosłych", icon: "📚" },
    { title: "Natura", desc: "Tutaj dzieje się magia! Cisza, spokój i oddanie.", icon: "🌳" },
    { title: "Tradycja", desc: "Od 1945 roku z pasją, szczerością i zaangażowaniem", icon: "🍎" },
]

// Pobieramy tylko 3 najnowsze posty
const recentPosts = await sanityClient.fetch(`*[_type == "post"] | order(publishedAt desc)[0..2] {
  title,
  slug,
  publishedAt,
  mainImage
}`);
---

<Layout title="Strona Główna">
    <section class="relative h-[80vh] flex items-center justify-center overflow-hidden">
        <div class="absolute inset-0 bg-gradient-to-b from-black/40 to-black/20 z-10"></div>
        <div class="absolute inset-0 bg-cover bg-center" style={`background-image: url('${heroImg.src}');`}></div>

        <div class="relative z-20 text-center p-8 max-w-4xl mx-auto bg-gray/20 backdrop-blur-sm rounded-2xl">
            <h1 class="font-serif text-4xl md:text-6xl font-bold text-white mb-6 drop-shadow-lg">
                Odkryj magię <br/> <span class="text-op-green">Owocowej Przystani</span>
            </h1>
            <p class="text-lg md:text-xl text-gray-100 mb-8 max-w-3xl mx-auto">
                Gospodarstwo sadowniczo-edukacyjne, gdzie natura spotyka się z nauką.
            </p>
            <div class="flex flex-col sm:flex-row gap-4 justify-center">
                <a href="/oferta/sady" class="px-8 py-3 bg-green-600 text-white rounded-full font-bold hover:bg-green-700 transition shadow-lg">
                    Oferta prosto z sadu
                </a>
                <a href="/oferta/warsztaty" class="px-8 py-3 bg-white text-op-brown rounded-full font-bold hover:bg-gray-200 transition shadow-lg">
                    Oferta edukacyjna
                </a>
            </div>
        </div>
    </section>

    <section id="o-nas" class="py-20 container mx-auto px-8">
        <div class="grid md:grid-cols-2 gap-12 items-center">
            <div class="space-y-6">
                <h2 class="font-serif text-3xl md:text-4xl font-bold text-op-brown">Witaj w naszym sadzie</h2>
                <p class="text-gray-600 leading-relaxed">
                    Jesteśmy gospodarstwem sadowniczym, gdzie z wiedzą trzech pokoleń sadowników, od 1945 roku nieprzerwanie z pasją i zaangażowaniem zajmujemy się produkcją owoców. Tradycja sadownicza rodziny Wierus trwa... ale chcąc dać Wam jeszcze więcej niż tylko owoce w 2018 roku powstaje nasza własna marka - <strong class="text-red-600">Owocowa Przystań</strong>.
                </p>
                <p class="text-gray-600 leading-relaxed">
                    Gospodarstwo sadownicze staje się również gospodarstwem edukacyjnym, a marzenie o stworzeniu miejsca przyjaznego dzieciom, gdzie mogłyby z radością i swobodą uczyć się poprzez zabawę i praktykę urzeczywistnia się. Chcemy aby to miejsce było inspiracją nie tylko dla dzieci, ale też dorosłych.
                </p>
            </div>
            <div class="grid grid-cols-2 gap-4">
                <Image src={workshopImg} alt="Warsztaty w sadzie" class="rounded-2xl shadow-md mt-8" />
                <Image src={workshop2Img} alt="Warsztaty w sadzie" class="rounded-2xl shadow-md" />
            </div>
        </div>
    </section>

    <section class="bg-white py-20">
        <div class="container mx-auto px-8">
            <div class="grid md:grid-cols-3 gap-8">
                {features.map(f => (
                    <div class="text-center p-8 rounded-2xl bg-op-cream border border-gray-100 hover:shadow-lg transition">
                        <div class="text-5xl mb-4">{f.icon}</div>
                        <h3 class="font-serif text-xl font-bold mb-2">{f.title}</h3>
                        <p class="text-gray-600">{f.desc}</p>
                    </div>
                ))}
            </div>
        </div>
    </section>

    <section class="py-20 bg-white border-t border-stone-100">
        <div class="container mx-auto px-4">

            <div class="flex justify-between items-end mb-12">
                <div>
                    <h2 class="text-3xl md:text-4xl font-serif font-bold text-stone-800 mb-2">
                        Co słychać w sadzie?
                    </h2>
                    <p class="text-stone-500">Najnowsze wieści z naszego gospodarstwa</p>
                </div>
                <a href="/blog" class="hidden md:inline-flex text-green-700 font-bold hover:underline">
                    Zobacz wszystkie wpisy →
                </a>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                {recentPosts.map((post) => (
                        <a href={`/blog/${post.slug.current}`} class="group block">
                            <div class="relative overflow-hidden rounded-xl aspect-[4/3] mb-4">
                                {post.mainImage ? (
                                        <img
                                                src={urlFor(post.mainImage).width(600).height(450).url()}
                                                alt={post.title}
                                                class="object-cover w-full h-full transform group-hover:scale-105 transition-transform duration-500"
                                        />
                                ) : (
                                        <div class="w-full h-full bg-stone-100 flex items-center justify-center text-stone-300">
                                            Brak zdjęcia
                                        </div>
                                )}
                                <div class="absolute inset-0 bg-black/0 group-hover:bg-black/10 transition-colors duration-300"></div>
                            </div>

                            <div class="text-sm text-green-600 mb-2">
                                {new Date(post.publishedAt).toLocaleDateString('pl-PL')}
                            </div>
                            <h3 class="text-xl font-bold text-stone-800 group-hover:text-green-700 transition-colors leading-tight">
                                {post.title}
                            </h3>
                        </a>
                ))}
            </div>

            <div class="mt-8 text-center md:hidden">
                <a href="/blog" class="inline-block px-6 py-3 border border-stone-300 rounded-full text-stone-600 font-medium hover:bg-stone-50 transition-colors">
                    Zobacz wszystkie wpisy
                </a>
            </div>

        </div>
    </section>
</Layout>
