---
import { Image } from 'astro:assets'
import heroImg from '../assets/apples.jpg'
import workshopImg from '../assets/sad_zajecia.jpg'
import workshop2Img from '../assets/sad_zajecia2.jpg'

import Layout from '../layouts/Layout.astro'

// Przykładowe dane "statyczne" do szybkiego startu, zanim backend zostanie uzupełniony
const features = [
    { title: "Edukacja", desc: "Warsztaty przyrodnicze dla szkół i przedszkoli.", icon: "📚" },
    { title: "Natura", desc: "Bezpośredni kontakt z przyrodą i życiem sadu.", icon: "🌳" },
    { title: "Tradycja", desc: "Poznanie pracy sadownika i cyklu pór roku.", icon: "🍎" },
]
---

<Layout title="Strona Główna">
    <section class="relative h-[80vh] flex items-center justify-center overflow-hidden">
        <div class="absolute inset-0 bg-gradient-to-b from-black/40 to-black/20 z-10"></div>
        <div class="absolute inset-0 bg-cover bg-center" style={`background-image: url('${heroImg.src}');`}></div>

        <div class="relative z-20 text-center px-8 max-w-4xl mx-auto">
            <h1 class="font-serif text-4xl md:text-6xl font-bold text-white mb-6 drop-shadow-lg">
                Odkryj magię <br/> <span class="text-op-green">Owocowej Przystani</span>
            </h1>
            <p class="text-lg md:text-xl text-gray-100 mb-8 max-w-3xl mx-auto">
                Gospodarstwo sadowniczo-edukacyjne, gdzie natura spotyka się z nauką.
            </p>
            <div class="flex flex-col sm:flex-row gap-4 justify-center">
                <a href="/oferta" class="px-8 py-3 bg-op-red text-white rounded-full font-bold hover:bg-red-700 transition shadow-lg">
                    Zobacz Ofertę Edukacyjną
                </a>
                <a href="/kontakt" class="px-8 py-3 bg-white text-op-brown rounded-full font-bold hover:bg-gray-200 transition shadow-lg">
                    Skontaktuj się
                </a>
            </div>
        </div>
    </section>

    <section id="o-nas" class="py-20 container mx-auto px-8">
        <div class="grid md:grid-cols-2 gap-12 items-center">
            <div class="space-y-6">
                <h2 class="font-serif text-3xl md:text-4xl font-bold text-op-brown">Witaj w naszym sadzie</h2>
                <p class="text-gray-600 leading-relaxed">
                    Jesteśmy gospodarstwem sadowniczo-edukacyjnym, które powstało z pasji do natury i chęci dzielenia się wiedzą.
                    Naszą misją jest pokazanie najmłodszym, że współpraca z naturą to klucz do zdrowia – zarówno naszego, jak i ekosystemu.
                </p>
                <p class="text-gray-600 leading-relaxed">
                    Prowadzimy zajęcia, podczas których dzieci nie tylko słuchają, ale dotykają, wąchają i budują.
                    Naszym przewodnikiem jest <strong>Szymon Wierus</strong> – sadownik z powołania.
                </p>
            </div>
            <div class="grid grid-cols-2 gap-4">
                <Image src={workshopImg} alt="Warsztaty w sadzie" class="rounded-2xl shadow-md mt-8" />
                <Image src={workshop2Img} alt="Warsztaty w sadzie" class="rounded-2xl shadow-md" />
            </div>
        </div>
    </section>

    <section class="bg-white py-20">
        <div class="container mx-auto px-8">
            <div class="grid md:grid-cols-3 gap-8">
                {features.map(f => (
                    <div class="text-center p-8 rounded-2xl bg-op-cream border border-gray-100 hover:shadow-lg transition">
                        <div class="text-5xl mb-4">{f.icon}</div>
                        <h3 class="font-serif text-xl font-bold mb-2">{f.title}</h3>
                        <p class="text-gray-600">{f.desc}</p>
                    </div>
                ))}
            </div>
        </div>
    </section>
</Layout>
